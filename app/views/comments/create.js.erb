$(document).ready( function () { console.log('Create comment called'); $('#comments-<%=@post.id%>').prepend('<%= j render "posts/comment", { comment: @comment} %>'); 
$('#comments-form-<%=@post.id%> textarea').val('');
});